# Baritone Helper 3.1.0 verification record

## Provenance note

The published annotated tag `v3.1.0` resolves to commit `caedde01babf2799c52b7b8396c94f02cee80ce6`.

The original 3.1 verification capture stored in the repository identified `85639f4179d77aa5422545b0ea60149d677bb118` as its reference revision and reported local pre-publish artifact sizes. That provenance does not match the exact published tag/artifacts, so the recorded test measurements below are retained as the **pre-publish 3.1 verification run**, not as a claim of binary-reproducible verification of the exact release assets.

Published GitHub release assets for `v3.1.0` were:

| Asset | Size | SHA-256 |
| --- | ---: | --- |
| `baritonehelper-3.1.0.jar` | 590,607 bytes | `5508d47f68f6d292fbe0e5c3f2da2f695e4d8563ac05299a4b8375c5f5599a56` |
| `baritonehelper-3.1.0-sources.jar` | 320,566 bytes | `c37e542d29d40ad1780687adb7e21c3cbc6b6682e7298b1ad1410b9bfd1a90e7` |

Future releases use the repository release workflow to validate tag/version/changelog consistency and publish explicit checksum files with release artifacts.

## Recorded pre-publish verification state

- Original reference revision: `85639f4179d77aa5422545b0ea60149d677bb118`
- Verification date: 2026-08-30
- Runtime: Eclipse Temurin Java 21

## Implemented architecture recorded by the 3.1 run

- Each active worker owns one long-lived relocated Baritone runtime and uses the canonical `MineProcess`; the legacy `WorkerPlanner.SearchCursor` is no longer on the production collection path.
- Palette-backed immutable world snapshots are captured on the server thread, scanned on a bounded two-thread executor, and consumed by the relocated Baritone pathfinder on a separate bounded four-thread executor.
- Scan requests are coalesced and generation-fenced. Cancellation, target replacement, and worker removal prevent stale asynchronous publication.
- Movement uses Baritone's player input math while applying the result to the server entity. Vanilla look and move controls cannot overwrite active Baritone output.
- Worker storage is the canonical inventory for mining, tool use, placement, drops, persistence, and the remote 27/54-slot dashboard.
- Every spawned worker owns a centered Chebyshev radius-12 view window: exactly 25 by 25, or 625, persistent view tickets plus one simulation anchor.

## Recorded automated results

| Check | Recorded result |
| --- | --- |
| JUnit architecture, concurrency, inventory, scanner, movement, and network contracts | **45/45 passed**, 0 failures, 0 errors |
| Dedicated-server GameTests | **40/40 passed** in 16.71 s in the profiled run |
| Four-worker aggregate orchestration p95 | **1.047599 ms** (budget: <= 2 ms) |
| Four-worker individual p95 values | 0.7208, 0.177399, 0.0802, and 0.0692 ms |
| Generation-fence manual mutation | **Killed** by `Release31ParityGameTests` |
| Java Flight Recorder | Valid 8 s profile, 1,108,359 bytes, JFR 2.1 |
| Local pre-publish runtime artifact recorded by the original document | `build/libs/baritonehelper-3.1.0.jar` (590,108 bytes) |
| Local pre-publish sources artifact recorded by the original document | `build/libs/baritonehelper-3.1.0-sources.jar` (323,818 bytes) |

The GameTest run covered movement parity fixtures, long-range loaded-world discovery, no-target rescan behavior, four-worker fairness, inventory ownership and lifecycle, chunk-window ownership and cleanup, and stale-publication fences.

The local profile referenced by the original record was `run/baritonehelper-3.1-gametest-20260830.jfr`; it reported `WorldScanner.scanPalette` at 1.23% of 244 execution samples and no helper pathfinding method above it in the hot-method view.

## Reproduction helpers

Run from the repository root with Java 21 selected through `BARITONEHELPER_JAVA` or `JAVA_HOME`:

```powershell
.\tools\gauntlet.ps1
.\tools\manual-mutation.ps1
```

The gauntlet performs a clean unit-test, dedicated-server GameTest, and build run, checks artifact cardinality/versioning, and rejects removed legacy packages. The mutation runner copies the project to a temporary directory, requires a green baseline, removes the async generation guard, and requires the parity tests to fail.

## Scope of the recorded performance claim

The measured p95 was worker orchestration time in the deterministic four-worker GameTest workload. The run was accelerated and was not a five-minute real-time 20 TPS soak, so the record does not claim a measured 19.5 TPS floor, a server-wide tick p95, or a production-host loaded-chunk pathfinding p95.
